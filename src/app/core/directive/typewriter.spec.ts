import { Typewriter } from './typewriter';

describe('Typewriter', () => {
  it('should create an instance', () => {
    const directive = new Typewriter();
    expect(directive).toBeTruthy();
  });
});
